{
    "emeraldwalk.runonsave": {
        "commands": [
            {
                "match": "\\.md$",
                "cmd": "pandoc -o ${fileBasenameNoExt}.pdf ${fileBasename} --defaults pandoc.yaml"
            },
        ]
    }
}